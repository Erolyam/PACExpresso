<div class="alineashow">
  <h1><?= $this->o->title ?> - <?= $this->o->chemNum ?></h1>

  <div class="context">
    <?= $this->o->context ?>
  </div>

  <hr class="context_body"/>

  <div class="body">
    <?= $this->o->body ?>
  </div>

  <div class="answers">
    <ul>
      <? $weight = 1; ?>
      <? $index = 0; ?>
      <? foreach($this->o->answers as $answer): ?>
        <?
          $checked = "";
          if ($this->o->solution & $weight) {
            $checked = "checked";
          }
          $weight <<= 1;
        ?>
        <li>
          <div class="ansltr">
            <?= chr(65 + $index) ?>.
          </div>
          <div class="anspcent">
            <? if ($this->o->count > 0): ?>
              <?= round($this->o->aChecked[$index] * 100 / $this->o->count); ?> %
            <? endif; ?>
          </div>
          <div class="anschk">
            <input type='checkbox' disabled <?= $checked ?>/>
          </div>
          <div class="ansbody">
            <?= $answer ?>
          </div>
        </li>
        <? $index++ ?>
      <? endforeach; ?>
    </ul>
  </div>
</div><!-- /.alineashow -->
